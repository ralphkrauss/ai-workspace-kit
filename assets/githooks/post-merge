#!/bin/sh
# Optional hook: sync generated AI workspace projections after merge or pull.

if command -v node >/dev/null 2>&1 && [ -f scripts/sync-ai-workspace.mjs ]; then
  node scripts/sync-ai-workspace.mjs >/dev/null 2>&1 || true
fi
